
                  <% if current_user.liked_by?(recipe.id) %>
                    <%= link_to destroy_like_path(recipe), method: :delete, remote: true do %>
                      <%= image_tag "heart.png", class:"iine_im" %>
                    <% end %>
                  <% else %>
                    <%= link_to create_like_path(recipe), method: :post, remote: true  do %>
                      <%= image_tag "hearthi.png", class:"iine_im" %>
                    <% end %>
                  <% end %>
                  <%= recipe.likes.count %>
